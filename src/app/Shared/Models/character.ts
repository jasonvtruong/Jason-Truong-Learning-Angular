export interface Character {
  id: number;
  name: string,
  age?: number,
  gender: string,
  riderName?: string,
  type: string,
  motif: string,
  finisherMove: string,
  debutDate: string,
  isARider?: boolean,
  imageName: string;
}
