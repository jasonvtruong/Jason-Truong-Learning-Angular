import { GenderColourPipe } from './gender-colour.pipe';

describe('GenderColourPipe', () => {
  it('create an instance', () => {
    const pipe = new GenderColourPipe();
    expect(pipe).toBeTruthy();
  });
});
